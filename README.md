# tfdiag
A terraform to diagram library


TODO:
Build a map for the diagrams - maybe automate via github actions?
Make it more flexible when parsing tf i.e.
    - handle different clouds
    - handle errors (icons that don't exist use a default)
Make it output to an editable format
Package it for local deployment
All the library shabang